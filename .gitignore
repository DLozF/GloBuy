.DS_Store
node_modules/
*.log
.env

# Local agent/dev state and Worker secrets — never commit.
.claude/
.agents/
skills-lock.json
.dev.vars
.wrangler/

# Store upload package (built from dist/).
*.zip
