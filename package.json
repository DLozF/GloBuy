{
  "name": "luxe-translate",
  "version": "0.1.0",
  "private": true,
  "description": "Tests for the Luxe Translate extension's pure detection helpers (findPrices, findSizes, inferSourceCurrency). No build step or runtime dependencies.",
  "scripts": {
    "test": "node --test"
  }
}
