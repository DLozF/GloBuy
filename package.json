{
  "name": "translate-extension",
  "version": "0.1.0",
  "description": "Chrome MV3 extension that translates foreign luxury/resale sites and converts prices inline.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.28",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
